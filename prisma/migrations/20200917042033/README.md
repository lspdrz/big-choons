# Migration `20200917042033`

This migration has been generated by Luis Pedraza at 9/16/2020, 11:20:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `big_choons`.`User` (
`id` int  NOT NULL  AUTO_INCREMENT,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`username` varchar(191)  NOT NULL ,
`name` varchar(191)  NOT NULL ,
`email` varchar(191)  NOT NULL ,
`userId` int  ,
`trackId` int  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `big_choons`.`Track` (
`id` int  NOT NULL  AUTO_INCREMENT,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`source` varchar(191)  NOT NULL ,
`type` ENUM('APPLE_MUSIC', 'SPOTIFY', 'SOUNDCLOUD', 'YOUTUBE')  NOT NULL DEFAULT 'YOUTUBE',
`notes` varchar(191)  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `big_choons`.`User` ADD FOREIGN KEY (`trackId`) REFERENCES `big_choons`.`Track`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `big_choons`.`User` ADD FOREIGN KEY (`userId`) REFERENCES `big_choons`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200917042033
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,43 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+enum MediaService {
+  APPLE_MUSIC
+  SPOTIFY
+  SOUNDCLOUD
+  YOUTUBE
+}
+
+model User {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @default(now())
+  username  String
+  name      String
+  email     String
+  friends   User[]   @relation("UserToUser")
+  track     Track    @relation(fields: [trackId], references: [id])
+  User      User?    @relation("UserToUser", fields: [userId], references: [id])
+  userId    Int?
+  trackId   Int
+}
+
+model Track {
+  id        Int          @default(autoincrement()) @id
+  createdAt DateTime     @default(now())
+  updatedAt DateTime     @default(now())
+  title     String
+  source    String
+  type      MediaService @default(YOUTUBE)
+  likes     User[]
+  notes     String
+}
```


