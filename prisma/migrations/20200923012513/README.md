# Migration `20200923012513`

This migration has been generated by Luis Pedraza at 9/22/2020, 9:25:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `big_choons`.`User` (
`id` int  NOT NULL  AUTO_INCREMENT,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`username` varchar(191)  NOT NULL ,
`name` varchar(191)  NOT NULL ,
`email` varchar(191)  NOT NULL ,
`trackId` int  ,
UNIQUE Index `User.email_unique`(`email`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `big_choons`.`Track` (
`id` int  NOT NULL  AUTO_INCREMENT,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`title` varchar(191)  NOT NULL ,
`source` varchar(191)  NOT NULL ,
`type` ENUM('APPLE_MUSIC', 'SPOTIFY', 'SOUNDCLOUD', 'YOUTUBE')  NOT NULL DEFAULT 'YOUTUBE',
`notes` varchar(191)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `big_choons`.`_UserFollows` (
`A` int  NOT NULL ,
`B` int  NOT NULL ,
UNIQUE Index `_UserFollows_AB_unique`(`A`,
`B`),
Index `_UserFollows_B_index`(`B`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `big_choons`.`User` ADD FOREIGN KEY (`trackId`) REFERENCES `big_choons`.`Track`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `big_choons`.`_UserFollows` ADD FOREIGN KEY (`A`) REFERENCES `big_choons`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `big_choons`.`_UserFollows` ADD FOREIGN KEY (`B`) REFERENCES `big_choons`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200923012513
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,42 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+enum MediaService {
+  APPLE_MUSIC
+  SPOTIFY
+  SOUNDCLOUD
+  YOUTUBE
+}
+
+model User {
+  id         Int      @default(autoincrement()) @id
+  createdAt  DateTime @default(now())
+  updatedAt  DateTime @default(now())
+  username   String
+  name       String
+  email      String   @unique
+  followedBy User[]   @relation("UserFollows", references: [id])
+  following  User[]   @relation("UserFollows", references: [id])
+  track      Track?   @relation(fields: [trackId], references: [id])
+  trackId    Int?
+}
+
+model Track {
+  id        Int          @default(autoincrement()) @id
+  createdAt DateTime     @default(now())
+  updatedAt DateTime     @default(now())
+  title     String
+  source    String
+  type      MediaService @default(YOUTUBE)
+  likes     User[]
+  notes     String?
+}
```


